kernel {
	module callout;
	module malloc;
	module mips;
	module sched;
	module systm;

	callout {
		options tsleep;
		options sched;
	};

	malloc {
		options fl;
		options fl_wrapper;
	};

	mips {
		options cache;
		options tlb;
		options qemu;
	};

	sched {
		options mutex;
		options malloc;

		nprio = 2;
	};

	systm {
		options console;
	};
};

library {
	module libc;
};
