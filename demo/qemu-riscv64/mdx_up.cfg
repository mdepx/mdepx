kernel {
	module callout;
	module malloc;
	module riscv;
	module systm;
	module sched;

	callout {
		options tsleep;
		options sched;
	};

	malloc {
		options fl;
		options fl_wrapper;
	};

	sched {
		options mutex;
		options malloc;

		nprio = 2;
	};

	systm {
		options console;
	};
};

library {
	module libc;
};
